#include "main.h"
/**
 *strchr - an entry point
 *@c:input
 *@s:input
 *
 * return: always 0
*/
char *_strchr(char *s, char c);
{
int = 0
	
for (; s[i] >= '\0'; i++)
{
if (s[i] == c)
return (&s[i]);
}
return (0);
}
